[package]
name = "kitetsu-primitives"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "kitetsu-primitives"
path = "src/main.rs"

[dependencies]
# Slint on a wlr-layer-shell surface. Early-dev (AGPL-3.0) — pinned by exact git rev per CLAUDE.md §8.
# layer-shika loads the .slint via the Slint interpreter at runtime, so no slint-build here.
layer-shika = { git = "https://github.com/waydeerwm/layer-shika", rev = "0a684334056e2ffb680ff38618ae1ceddb319218" }
